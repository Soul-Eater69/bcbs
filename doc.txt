
Also change `ValueStreamService.get_ppt_text(...)` so it cleans after extraction instead of returning raw MarkItDown text:

```python
from core.text import clean_ppt_text

text = self._md.convert(str(path)).text_content or ""
text = clean_ppt_text(text)
if not text.strip():
    raise ValueError(f"No text could be extracted from {path}.")
safe_text = truncate_to_token_limit(text)
return safe_text
